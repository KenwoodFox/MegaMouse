/**
 * @file algorithm.h
 * @author Joe S
 * @brief Prototypes for the micromouse algorithm
 */

#include <cstdint> // This include might have to be moved..

class Algorithm
{
private:
    uint16_t maze[16][16];

public:
    // Algorithm(/* args */);

    void init();
};
